[package]
name = "blvm-sdk-macros"
version = "0.1.0"
edition = "2021"
description = "Procedural macros for BLVM SDK CLI module system"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full", "parsing"] }
quote = "1.0"
proc-macro2 = "1.0"
